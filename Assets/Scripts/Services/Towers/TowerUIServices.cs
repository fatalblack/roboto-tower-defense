using UnityEngine;

public class TowerUIServices : MonoBehaviour
{
    // private variables
    //private Vector3 originalPosition;

    // Start is called before the first frame update
    private void Start()
    {
        // set the original position to be used if the tower is dropped in a invalid place
        //originalPosition = transform.position;
    }

    // Update is called once per frame
    private void Update()
    {
        
    }
}